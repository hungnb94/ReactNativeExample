import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#8dffc4',
        alignItems: 'center',
        justifyContent: 'center',
    },
    item: {
        height: 40,
        borderColor: '#2a1dfe',
        width: 120,
    }
});

export default styles;